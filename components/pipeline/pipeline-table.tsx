// Legacy compact pipeline table — superseded by:
//   - components/pipeline/pipeline-surface.tsx (shell)
//   - components/pipeline/pipeline-board.tsx   (Kanban view)
//   - components/pipeline/pipeline-list.tsx    (slim list view)
//
// The old PipelineTable export is no longer imported anywhere. Kept this stub
// file in place rather than deleting it so external bookmarks / cached imports
// during the redeploy don't break. Safe to delete in a future cleanup.

export {};
